# encoding
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force-response=true

# log
logging.level.root=info

# 데이터베이스에 배치 스키마가 존재하지 않는다면 자동으로 배치 스키마를 생성하도록 처리
spring.batch.initialize-schema=always

# DB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://49.247.213.4/HAJULA?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=app_id
spring.datasource.password=appHajula123!

spring.jpa.show-sql=true
spring.jpa.database=mysql
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.generate-ddl=true
